import { StyleSheet } from 'react-native';

const counterScreenStyles = StyleSheet.create({
  bottom: {
    paddingBottom: 8,
  },
  headerFont: {
    fontSize: 24,
  },
  container: {
    padding: 8,
  },
  section: {
    paddingTop: 16,
  },
});

export default counterScreenStyles;
