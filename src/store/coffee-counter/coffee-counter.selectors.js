export const _selectQue = (que) => {
  return que;
};

export const _selectIsWorking = (isWorking) => {
  return isWorking;
};

export const _selectCountdown = (countdown) => {
  return countdown;
};

export const _selectFinished = (finished) => {
  return finished;
};
